<article class="post">
    <div class="title">
        <h1><a href="/post/{{id}}">{{title}}</a></h1>
        <p>{{post_content}}</p>
        <p>Posted: {{format_date created_at}} by {{user.username}}</p>
    </div>
    {{!-- <div class="meta">
        {{vote_count}} point(s) by {{user.username}} on
        {{created_at}}
        |
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
    </div> --}}
</article>